import React from "react";
import { Frame } from "arwes";

const Splash = () => (
  <Frame show={true} animate={true} level={3} corners={4} layer="primary">
    <div style={{ padding: "20px 40px", fontSize: "32px" }}>Go nuts, Abdullah!</div>
  </Frame>
);

export default Splash;
